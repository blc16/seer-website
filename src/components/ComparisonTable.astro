---
import { benchmarks, monthlyEvalPricing } from '../content/copy';
---
<div class="space-y-6">
  <table class="w-full text-sm card p-6">
    <thead class="text-left text-muted">
      <tr>
        <th class="py-2">Model</th>
        <th>Accuracy</th>
        <th>Macro F1</th>
        <th>Micro F1</th>
      </tr>
    </thead>
    <tbody>
      {benchmarks.map((b) => (
        <tr>
          <td>{b.model}</td>
          <td>{b.accuracy.toFixed ? b.accuracy.toFixed(3) : b.accuracy}</td>
          <td>{b.macroF1}</td>
          <td>{b.microF1}</td>
        </tr>
      ))}
    </tbody>
  </table>

  <table class="w-full text-sm card p-6">
    <thead class="text-left text-muted">
      <tr>
        <th class="py-2">Monthly Evals</th>
        <th>Seer-4B (low–high)</th>
        <th>Seer-1.7B COGS (low–high)</th>
        <th class="text-right">GPT-5</th>
        <th class="text-right">GPT-5-mini</th>
        <th class="text-right">GPT-5-nano</th>
        <th class="text-right">Claude 3.5/4 Sonnet</th>
        <th class="text-right">Claude 4 Opus</th>
      </tr>
    </thead>
    <tbody>
      {monthlyEvalPricing.map((row) => (
        <tr>
          <td>{row.monthlyEvals}</td>
          <td>{row.seer4BLowHigh}</td>
          <td>{row.seer1_7BCOGSLowHigh}</td>
          <td class="text-right">{row.gpt5}</td>
          <td class="text-right">{row.gpt5Mini}</td>
          <td class="text-right">{row.gpt5Nano}</td>
          <td class="text-right">{row.claudeSonnet}</td>
          <td class="text-right">{row.claudeOpus}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
