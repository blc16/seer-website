---
const { value, label } = Astro.props;
---
<div class="card p-6 text-center">
  <div class="text-3xl font-semibold text-text">{value}</div>
  <div class="mt-2 text-sm text-muted">{label}</div>
</div>
