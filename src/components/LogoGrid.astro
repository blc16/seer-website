---
const { logos = [] } = Astro.props;
---
<div class="container py-14">
  <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-6 items-center">
    {logos.map((logo) => (
      <img src={logo.src} alt={logo.alt} class="h-10 object-contain" loading="lazy" />
    ))}
  </div>
</div>
