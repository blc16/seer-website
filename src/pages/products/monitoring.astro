---
import Base from '../../layouts/Base.astro';
import Section from '../../components/Section.astro';
import CTA from '../../components/CTA.astro';
---
<Base title="Production Monitoring — Seer" description="Know when your RAG breaks. Get alerts within minutes of quality dropping.">
  <section class="container grid lg:grid-cols-2 gap-10 items-center py-16">
    <div>
      <h1 class="font-serif text-5xl">Know when your RAG breaks.</h1>
      <p class="mt-4 text-lg text-muted">Track recall, precision, and latency on every query. Get Slack alerts within 5 minutes of quality dropping—before users notice.</p>
      <div class="mt-8">
        <a href="https://docs.seersearch.com/seer/production-monitoring" class="text-primary hover:underline">Learn how it works →</a>
      </div>
    </div>
    <img src="/assets/monitoring-dashboard.png" alt="Monitoring dashboard" class="rounded-2xl shadow-card" loading="eager" />
  </section>

  <Section title="What you see">
    <div class="grid md:grid-cols-2 gap-6">
      <img src="/assets/monitoring-dashboard.png" alt="Quality metrics" class="card" loading="lazy" />
      <img src="/assets/monitoring-latency.png" alt="Latency breakdown" class="card" loading="lazy" />
    </div>
    <div class="grid md:grid-cols-3 gap-6 mt-6 text-sm">
      <div class="card p-5">
        <p class="font-semibold">Recall & Precision</p>
        <p class="text-muted mt-1">See what % of relevant docs you're retrieving</p>
      </div>
      <div class="card p-5">
        <p class="font-semibold">Latency P50/P95</p>
        <p class="text-muted mt-1">Track retrieval speed over time</p>
      </div>
      <div class="card p-5">
        <p class="font-semibold">Query volume</p>
        <p class="text-muted mt-1">Monitor traffic patterns</p>
      </div>
    </div>
  </Section>

  <Section title="Alerting">
    <div class="grid md:grid-cols-3 gap-6 text-sm">
      <div class="card p-6">
        <p class="font-semibold">Slack & webhooks</p>
        <p class="text-muted mt-2">Get notified instantly when recall drops below threshold</p>
      </div>
      <div class="card p-6">
        <p class="font-semibold">5-minute detection</p>
        <p class="text-muted mt-2">Catch regressions before they impact users</p>
      </div>
      <div class="card p-6">
        <p class="font-semibold">Per-environment</p>
        <p class="text-muted mt-2">Separate alerts for prod, staging, dev</p>
      </div>
    </div>
  </Section>

  <Section title="No labels required">
    <p class="text-muted max-w-2xl">Seer evaluates context quality automatically using a fine-tuned 4B parameter model. It understands whether your retrieved docs actually answer the query—no manual annotation needed.</p>
    <div class="grid md:grid-cols-2 gap-6 mt-6 text-sm">
      <div class="card p-5">
        <p class="font-semibold">0.87 F1 on benchmarks</p>
        <p class="text-muted mt-1">Matches GPT-4 accuracy at 38x lower cost</p>
      </div>
      <div class="card p-5">
        <p class="font-semibold">&lt;200ms latency</p>
        <p class="text-muted mt-1">Fast enough to evaluate every production query</p>
      </div>
    </div>
  </Section>

  <CTA 
    title="Start monitoring in 10 minutes" 
    description="Add 5 lines of code. See your first metrics."
    cta="Read the docs"
    href="https://docs.seersearch.com/seer/quickstart" />
</Base>
